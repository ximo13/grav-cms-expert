name: Premium Theme
slug: premium-theme
type: theme
version: 1.0.0

form:
  validation: strict

  fields:

    enabled:
      type: toggle
      label: Theme Status
      highlight: 1
      default: 1
      options:
        1: Enabled
        0: Disabled
      validate:
        type: bool
