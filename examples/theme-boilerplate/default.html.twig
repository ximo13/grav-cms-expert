{% extends 'base.html.twig' %}

{% block content %}

<section class="section">
    <div class="container">

        <h1>{{ page.title }}</h1>

        <div class="content">
            {{ page.content|raw }}
        </div>

    </div>
</section>

{% endblock %}
